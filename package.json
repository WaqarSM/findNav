{
  "name": "findnav",
  "version": "0.1.0",
  "private": true,
  "description": "A keyboard-first Chrome extension for finding and activating page links and controls.",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && node scripts/copy-assets.mjs",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
